export const metadata = {
  title: "Redirecting to Play Store - Fynace",
  description: "Download Fynace on the Google Play Store to start tracking your finances.",
};

import RedirectClient from "./RedirectClient";

export default function Page() {
  return <RedirectClient />;
}
